name: Tests

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

env:
  CARGO_TERM_COLOR: always

jobs:
  test:
    name: Test (${{ matrix.os }} / ${{ matrix.rust }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            rust: stable
          - os: macos-latest
            rust: stable
          - os: windows-latest
            rust: stable
          - os: ubuntu-latest
            rust: beta

    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@master
        with:
          toolchain: ${{ matrix.rust }}
          components: rustfmt, clippy

      - uses: Swatinem/rust-cache@v2

      - name: Check formatting
        run: cargo fmt -- --check

      - name: Lint with clippy
        run: cargo clippy -- -D warnings

      - name: Build
        run: cargo build --verbose

      - name: Run tests
        # Covers lib tests, in-binary #[cfg(test)] modules, and the
        # cli_tests / unit_tests integration files in tests/.
        run: cargo test --all-targets --verbose

      - name: Test CLI functionality
        run: |
          cargo run -- --help
          cargo run -- ls
          cargo run -- current

      - name: Build release
        run: cargo build --release --verbose

  coverage:
    runs-on: ubuntu-latest
    # Coverage is informational. Tarpaulin under --follow-exec is slow
    # enough to flake on subprocess-heavy tests; we don't want that to
    # gate the build. Codecov still gets the report when it succeeds.
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - uses: Swatinem/rust-cache@v2

      - name: Install cargo-tarpaulin
        uses: taiki-e/install-action@v2
        with:
          tool: cargo-tarpaulin

      - name: Run tests with coverage
        # --follow-exec lets tarpaulin trace into binaries spawned by
        # assert_cmd in tests/cli_tests.rs. Without it, anything those
        # tests exercise looks uncovered.
        # --timeout 300: instrumented tests are slower; the default 60s
        # is tight for our CLI tests that spawn shell-script stubs.
        # No --fail-under: tarpaulin still under-reports for subprocess
        # code, so a hard gate is theatre. We track the trend on
        # Codecov instead.
        run: cargo tarpaulin --follow-exec --timeout 300 --out Xml --out Stdout

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v5
        with:
          files: ./cobertura.xml
          flags: rust
          name: codecov-rust

  security:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Install cargo-audit
        uses: taiki-e/install-action@v2
        with:
          tool: cargo-audit

      - name: Security audit
        run: cargo audit

  msrv:
    name: Minimum Supported Rust Version
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust 1.86
        uses: dtolnay/rust-toolchain@master
        with:
          toolchain: 1.86.0

      - uses: Swatinem/rust-cache@v2

      - name: Check MSRV
        run: cargo check
